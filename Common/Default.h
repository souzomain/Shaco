#ifndef _DEFAULT_H
#define _DEFAULT_H

#include "Macros.h"
#include "Defines.h"

int s_atoi(char *str, bool *ok);
char *generate_random_str(int max);
int generate_random_int(int min, int max);

#endif
